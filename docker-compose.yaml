services:
  api:
    build: .
    ports:
      - "8000:8080"
    volumes:
      - .:/app
    command: uv run uvicorn infraprobe.app:app --host 0.0.0.0 --port 8080 --reload
    environment:
      - ENV=development
